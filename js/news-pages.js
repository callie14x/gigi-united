function esc(v=''){return String(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;')}
function fmt(v){return new Intl.DateTimeFormat('nl-NL',{day:'numeric',month:'long',year:'numeric'}).format(new Date(v))}
function rootPath(){return document.body.dataset.root||'../'}
async function getNews(){const r=await fetch(rootPath()+'data/news.json',{cache:'no-store'});if(!r.ok)throw new Error('Nieuws kon niet worden geladen');return r.json()}
function articleUrl(slug){return rootPath()+'pages/nieuwsartikel.html?artikel='+encodeURIComponent(slug)}
async function renderNewsPage(){const target=document.querySelector('#all-news');if(!target)return;const posts=(await getNews()).sort((a,b)=>new Date(b.date)-new Date(a.date));target.innerHTML=posts.map(p=>`<a class="all-news-card" href="${articleUrl(p.slug)}"><img src="${rootPath()+esc(p.image)}" alt="${esc(p.imageAlt||'')}"><div><span>${esc(p.category)}</span><time>${fmt(p.date)}</time><h2>${esc(p.title)}</h2><p>${esc(p.summary)}</p><strong>Lees verder →</strong></div></a>`).join('')}
async function renderArticle(){const target=document.querySelector('#news-article');if(!target)return;const slug=new URLSearchParams(location.search).get('artikel');const posts=await getNews();const p=posts.find(x=>x.slug===slug);if(!p){target.innerHTML='<div class="article-missing"><h1>Artikel niet gevonden</h1><a href="nieuws.html">Terug naar nieuws</a></div>';return}document.title=`${p.title} | Gigi-United`;target.innerHTML=`<header class="article-header"><span>${esc(p.category)}</span><time>${fmt(p.date)}</time><h1>${esc(p.title)}</h1><p>${esc(p.summary)}</p></header><img class="article-hero-image" src="${rootPath()+esc(p.image)}" alt="${esc(p.imageAlt||'')}"><div class="article-body">${(p.content||[]).map(x=>`<p>${esc(x)}</p>`).join('')}</div>${p.gallery?.length?`<div class="article-gallery">${p.gallery.map(x=>`<img src="${rootPath()+esc(x)}" alt="">`).join('')}</div>`:''}<div class="article-back"><a href="nieuws.html">← Terug naar al het nieuws</a></div>`}
renderNewsPage().catch(console.error);renderArticle().catch(console.error);